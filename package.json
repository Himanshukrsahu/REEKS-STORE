{
  "name": "reeksto-workspace",
  "version": "1.0.0",
  "description": "Root workspace workspace for Reeksto premium skincare e-commerce platform",
  "scripts": {
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client --legacy-peer-deps",
    "install:all": "npm run install:server && npm run install:client",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "start npm run server ^& start npm run client"
  }
}
